# Absolute URL of the deployed site. Used only to resolve absolute
# URLs for the OpenGraph and Twitter Card images. Has no effect on
# behavior — defaults to https://dropout.local when unset, which
# generates a broken (but harmless) social-preview URL.
NEXT_PUBLIC_SITE_URL=https://your-domain.example
